# exif-check · session guide

1. Read `ARGUS/00-core/standards/ONBOARD.md` before anything else.
2. Read `.project.yml` here; `policy.notes_for_agent` holds the warning.
3. This session modifies ONLY this folder. Other repos are out of scope.
4. Nothing deploys, publishes or changes copy tone without Luis (STD-022).
5. Before closing: run `bash ../../00-core/brand/check-rules.sh`.
